<% if @pages.any? %>
  <% @pages.each do |page| %>
    <%= render partial: 'shared/page',
                locals: {
                page: page,
                content: page.summary
                } %>
  <% end %>
<% else %>
  <p>No results found</p>
<% end %>